import { Gauge, Fuel, Route, Wind } from 'lucide-react'
const FuelOptimization = ({ data }) => { const s=data?.speed_opt||{}; const vals=[['Optimal Speed',s.speed==null?'—':`${s.speed.toFixed(1)} kn`,Gauge],['Fuel Consumption',s.fuel_per_day==null?'—':`${s.fuel_per_day.toFixed(1)} t/day`,Fuel],['Total Fuel',s.total_fuel==null?'—':`${s.total_fuel.toFixed(1)} t`,Fuel],['Voyage Time',s.voyage_hours==null?'—':`${(s.voyage_hours/24).toFixed(1)} days`,Route]]; return <section className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm sm:p-6"><div className="mb-5"><div className="flex items-center gap-2"><Gauge size={18} className="text-sky-600"/><h2 className="text-base font-black text-[#0a2440]">Fuel & Speed Optimization</h2></div><p className="mt-1 text-xs text-slate-500">XGBoost fuel model evaluates operating speeds from 10–20 knots.</p></div><div className="grid grid-cols-2 gap-3 lg:grid-cols-4">{vals.map(([label,value,Icon])=><div key={label} className="rounded-xl border border-slate-200 p-4"><Icon size={17} className="text-sky-600"/><p className="mt-3 text-[10px] font-extrabold uppercase tracking-wide text-slate-500">{label}</p><p className="mt-1 text-lg font-black text-[#0a2440]">{value}</p></div>)}</div><div className="mt-4 flex gap-3 rounded-xl bg-slate-50 p-4"><Wind size={18} className="shrink-0 text-slate-500"/><div><p className="text-xs font-bold text-slate-700">Weather-aware optimization</p><p className="mt-1 text-xs leading-5 text-slate-500">Wind speed and wave height are passed into the fuel model for the selected voyage scenario.</p></div></div></section> }
export default FuelOptimization