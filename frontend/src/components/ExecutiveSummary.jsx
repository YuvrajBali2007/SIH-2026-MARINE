import { ArrowRight, CheckCircle2, FileText, Sparkles } from 'lucide-react'
const ExecutiveSummary = ({ data, parameters }) => { const t=data?.timing_decision||{}; const pc=data?.port_constraints||{}; const allowed=pc.allowed_classes?.includes(parameters.vessel_class); return <section className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm sm:p-6"><div className="flex items-center justify-between"><div className="flex items-center gap-2"><FileText size={18} className="text-sky-600"/><div><h2 className="text-base font-black text-[#0a2440]">Executive Summary</h2><p className="mt-1 text-xs text-slate-500">Decision-ready snapshot for procurement review</p></div></div><Sparkles size={18} className="text-orange-500"/></div><div className="mt-5 grid gap-4 lg:grid-cols-3"><Block title="Chartering Decision"><p className="text-xl font-black text-[#0a2440]">{t.decision==='DEFER'?'Defer Fixture':'Execute Now'}</p><p className="mt-1 text-xs leading-5 text-slate-500">{t.decision_label||'—'}</p></Block><Block title="Port Readiness"><div className="flex items-center gap-2"><CheckCircle2 className={allowed?'text-emerald-500':'text-red-500'} size={18}/><p className="text-lg font-black text-[#0a2440]">{allowed?'Class Allowed':'Class Review'}</p></div><p className="mt-1 text-xs text-slate-500">{parameters.port} · {parameters.vessel_class}</p></Block><Block title="Model Signal"><p className="text-xl font-black text-[#0a2440]">T+{data?.optimal_t_star ?? '—'}</p><p className="mt-1 text-xs text-slate-500">Estimated waiting saving: ${Number(t.savings_if_wait||0).toLocaleString()}</p></Block></div><div className="mt-4 flex items-center justify-between rounded-xl bg-slate-50 p-4"><div><p className="text-xs font-bold text-slate-700">Procurement workflow</p><p className="mt-1 text-xs text-slate-500">Validate market quotes, port confirmations and commercial approvals before execution.</p></div><ArrowRight size={18} className="text-slate-400"/></div></section> }
const Block=({title,children})=><div className="rounded-xl border border-slate-200 p-4"><p className="text-[10px] font-extrabold uppercase tracking-wide text-slate-500">{title}</p><div className="mt-3">{children}</div></div>
export default ExecutiveSummary