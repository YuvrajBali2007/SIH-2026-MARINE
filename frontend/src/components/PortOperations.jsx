import { Anchor, CheckCircle2, CircleAlert, Gauge, Clock3 } from 'lucide-react'
const PortOperations = ({ data, parameters }) => { const pc=data?.port_constraints||{}; const cls=parameters?.vessel_class||'Panamax'; const allowed=pc.allowed_classes?.includes(cls); const draftMap={Handysize:9.5,Supramax:12.5,Panamax:14.2,Capesize:18.5}; const draft=draftMap[cls]; const compliant=allowed && draft <= pc.max_draft && parameters.vessel_dwt <= pc.max_dwt; return <section className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm sm:p-6"><div className="mb-5 flex items-center gap-2"><Anchor size={18} className="text-sky-600"/><div><h2 className="text-base font-black text-[#0a2440]">Port & Berth Operations</h2><p className="mt-1 text-xs text-slate-500">Draft, DWT, queue and operational constraint assessment</p></div></div><div className={`rounded-xl border p-4 ${compliant?'border-emerald-200 bg-emerald-50':'border-red-200 bg-red-50'}`}><div className="flex gap-3">{compliant?<CheckCircle2 className="text-emerald-600"/>:<CircleAlert className="text-red-600"/>}<div><p className={`text-sm font-black ${compliant?'text-emerald-700':'text-red-700'}`}>{compliant?'Port Compatible':'Constraint Review Required'}</p><p className="mt-1 text-xs text-slate-600">{compliant?'Selected vessel class, draft and DWT clear the configured port limits.':'Selected scenario exceeds one or more configured port constraints.'}</p></div></div></div><div className="mt-4 grid grid-cols-2 gap-3 lg:grid-cols-4"><Metric icon={Anchor} label="Port" value={parameters.port}/><Metric icon={Gauge} label="Draft" value={`${draft.toFixed(1)} m`}/><Metric icon={Gauge} label="Max Draft" value={`${Number(pc.max_draft||0).toFixed(1)} m`}/><Metric icon={Clock3} label="Queue" value={`${Number(pc.queue_days_avg||0).toFixed(1)} days`}/></div><div className="mt-4 flex items-center justify-between rounded-xl bg-slate-50 p-4"><span className="text-xs font-semibold text-slate-500">Demurrage exposure</span><strong className="text-sm text-[#0a2440]">${Number(pc.demurrage_usd_day||0).toLocaleString()} / day</strong></div></section> }
const Metric=({icon:Icon,label,value})=><div className="rounded-xl border border-slate-200 p-4"><Icon size={15} className="text-slate-500"/><p className="mt-2 text-[10px] font-extrabold uppercase tracking-wide text-slate-500">{label}</p><p className="mt-1 text-sm font-black text-[#0a2440]">{value}</p></div>
export default PortOperations