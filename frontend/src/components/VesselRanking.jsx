import { Award, Fuel, Ship, Gauge } from 'lucide-react'
const VesselRanking = ({ data }) => { const rows=Array.isArray(data?.ranked_vessels)?data.ranked_vessels:[]; return <section className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm sm:p-6"><div className="mb-5 flex items-center gap-2"><Ship size={18} className="text-sky-600"/><div><h2 className="text-base font-black text-[#0a2440]">Vessel Allocation</h2><p className="mt-1 text-xs text-slate-500">Top candidates ranked by capacity, speed, fuel efficiency, charter cost and age</p></div></div>{rows.length?<div className="overflow-x-auto"><table className="w-full min-w-[720px] text-left"><thead><tr className="border-b border-slate-200 text-[10px] font-extrabold uppercase tracking-wide text-slate-500"><th className="px-3 py-3">#</th><th className="px-3 py-3">Vessel</th><th className="px-3 py-3">Type</th><th className="px-3 py-3">DWT</th><th className="px-3 py-3">Age</th><th className="px-3 py-3">Charter/day</th><th className="px-3 py-3">Fuel Eff.</th><th className="px-3 py-3">Score</th></tr></thead><tbody>{rows.slice(0,8).map((v,i)=><tr key={`${v.vessel_id}-${i}`} className="border-b border-slate-100 last:border-0 hover:bg-slate-50"><td className="px-3 py-3 text-xs font-black text-slate-400">{v.rank||i+1}</td><td className="px-3 py-3 text-xs font-bold text-[#0a2440]">{v.vessel_id||v.candidate_id||'—'}</td><td className="px-3 py-3 text-xs text-slate-600">{v.vessel_type||'—'}</td><td className="px-3 py-3 text-xs font-semibold">{Number(v.dwt||0).toLocaleString()}</td><td className="px-3 py-3 text-xs">{v.vessel_age ?? '—'}</td><td className="px-3 py-3 text-xs">${Number(v.charter_rate_usd_day||0).toLocaleString()}</td><td className="px-3 py-3 text-xs">{Number(v.fuel_efficiency||0).toFixed(4)}</td><td className="px-3 py-3"><span className="inline-flex items-center gap-1 rounded-full bg-sky-50 px-2 py-1 text-xs font-black text-sky-700"><Award size={12}/>{Number(v.vessel_score||0).toFixed(1)}</span></td></tr>)}</tbody></table></div>:<div className="rounded-xl border border-red-200 bg-red-50 p-5"><p className="text-sm font-black text-red-700">No suitable vessel candidates</p><p className="mt-1 text-xs text-red-600">The selected cargo, vessel class, draft or transit assumptions do not produce a suitable candidate in the current vessel dataset.</p></div>}<div className="mt-4 grid grid-cols-2 gap-3 sm:grid-cols-4"><Mini icon={Ship} label="Candidates" value={rows.length}/><Mini icon={Gauge} label="Top Score" value={rows[0]?Number(rows[0].vessel_score).toFixed(1):'—'}/><Mini icon={Fuel} label="Fuel Metric" value={rows[0]?Number(rows[0].fuel_efficiency).toFixed(4):'—'}/><Mini icon={Award} label="Ranking" value="Multi-factor"/></div></section> }
const Mini=({icon:Icon,label,value})=><div className="rounded-xl bg-slate-50 p-3"><Icon size={14} className="text-slate-500"/><p className="mt-2 text-[9px] font-extrabold uppercase text-slate-500">{label}</p><p className="mt-1 text-xs font-black text-[#0a2440]">{value}</p></div>
export default VesselRanking